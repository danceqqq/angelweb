# Angel Web - Личная страница

Минималистичная личная страница с лентой постов и ссылками, выполненная в духе Vastlyra.

## Особенности

- 🎨 Минималистичный дизайн в стиле Vastlyra
- 📱 Две страницы: Лента постов и MyLinks
- 👆 Переключение страниц свайпом или кликом на правую/левую часть экрана
- 📹 Поддержка видео в стиле YouTube Shorts с автоплеем
- 🖼️ Поддержка изображений в постах
- 🔗 Страница ссылок с иконками
- 📱 Адаптивный дизайн

## Структура

- `index.html` - основная HTML структура
- `styles.css` - стили в минималистичном стиле
- `script.js` - логика переключения страниц, рендеринг постов и ссылок

## Настройка

### Добавление постов

Отредактируйте массив `postsData` в `script.js`:

```javascript
const postsData = [
    {
        id: 1,
        author: "Ваше имя",
        avatar: "URL_к_аватарке",
        date: "2 часа назад",
        content: "Текст поста",
        media: {
            type: "video", // или "image"
            url: "URL_к_видео_или_изображению"
        }
    }
];
```

### Добавление ссылок

Отредактируйте массив `linksData` в `script.js`:

```javascript
const linksData = [
    {
        id: 1,
        title: "Название",
        description: "Описание",
        url: "https://ссылка.com",
        icon: "github" // доступные: github, linkedin, twitter, instagram, portfolio, email
    }
];
```

### Добавление SVG иконок

Добавьте новые иконки в объект `icons` в `script.js`:

```javascript
const icons = {
    // ... существующие иконки
    newIcon: `<svg viewBox="0 0 24 24">...</svg>`
};
```

## Развертывание на GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите файлы в репозиторий
3. В настройках репозитория включите GitHub Pages
4. Выберите ветку `main` и папку `/root`
5. Ваш сайт будет доступен по адресу: `https://username.github.io/repository-name`

## Локальный запуск

Просто откройте `index.html` в браузере или используйте локальный сервер:

```bash
# Python
python -m http.server 8000

# Node.js
npx serve
```

## Лицензия

MIT

